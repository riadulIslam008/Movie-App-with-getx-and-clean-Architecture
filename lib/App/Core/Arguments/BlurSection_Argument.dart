import 'package:flutter/material.dart';

class BlurSectionArgument{
    final Color color;
  final double sigmaX;
  final double sigmaY;

  BlurSectionArgument(this.color, this.sigmaX, this.sigmaY);
}